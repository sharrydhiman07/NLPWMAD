name: food_classifier
description: A Flutter app for food classification using PyTorch model.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2   # HTTP library for making requests
  image_picker: ^1.1.2  # To pick images from gallery or camera

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
